import { apiProductResponse } from "./Product";

// export default function apiGetMethod = jest.fn().mockImplementation(
//     new Promise((resolve, reject) => {
//         process.nextTick(
//             () => resolve(apiProductResponse);
//         );
//     });
// );
